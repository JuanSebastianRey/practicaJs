export default function getRandomInt(min, max,a){
    Math.floor(Math.random() * (max - min + 1)) + min;
    let nmm = getRandomInt
    return nmm === a ? "Felicitaciones, ese era!" : "Lo siento, intenta nuevamente!"
    
  }